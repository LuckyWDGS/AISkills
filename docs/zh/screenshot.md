# screenshot

`screenshot` 用来截取桌面、窗口、应用或指定区域的系统截图，适合没有更专用截图工具时使用。

## 什么时候用

- 你明确要求截屏
- 目标不是浏览器，Playwright 等工具不适合
- 需要截整个桌面、某个 app 窗口或屏幕区域
- Codex 需要临时截图来检查视觉问题

## 重点

- 用户指定路径时保存到指定路径
- 用户只说截图时使用系统默认截图位置
- Codex 自己检查用的临时截图放到临时目录
